MINI PROJECT FOR (PYTHON FUNDAMENTAL)

# PROJECT - 1
SOLUTION:- 
n = int(input("how far would you like to travel in miles ? "))
if n <= 3:
    print("I suggest you ride a bicycle ")
elif n <= 300:
    print("I suggest you to ride a motor-cycle.")
else:
    print("I suggest super-car to your destination")       


# PROJECT - 2
SOLUTION:-
# Constants for calculation
COST_PER_HOUR = 0.51
HOURS_PER_DAY = 24
DAYS_PER_WEEK = 7
DAYS_PER_MONTH = 30  # Standard billing assumption
TOTAL_BUDGET = 918

# 1. Calculate costs
cost_per_day = COST_PER_HOUR * HOURS_PER_DAY
cost_per_week = cost_per_day * DAYS_PER_WEEK
cost_per_month = cost_per_day * DAYS_PER_MONTH

# 2. Calculate maximum operation days with budget
max_days = TOTAL_BUDGET / cost_per_day

# Display answers
print(f"How much does it cost to operate one server per day?")
print(f"Answer: ${cost_per_day:.2f}\n")

print(f"How much does it cost to operate one server per week?")
print(f"Answer: ${cost_per_week:.2f}\n")

print(f"How much does it cost to operate one server per month?")
print(f"Answer: ${cost_per_month:.2f}\n")

print(f"How many days can I operate one server with $918?")
print(f"Answer: {max_days:.1f} days")



MINI PROJECT FOR (DATA STRUCTURE)

PROJECT 1:-  (DICTIONARY)

# Create a dictionary of people and their interesting facts
people = {
    "Jeff": "Is afraid of Dogs.",
    "David": "Plays the piano.",
    "Jason": "Can fly an airplane."
}

# Display the original dictionary
print("Original List:")
for name, fact in people.items():
    print(name + ":", fact)

# Change a fact about Jeff
people["Jeff"] = "Is afraid of heights."

# Add a new person
people["Jill"] = "Can hula dance."

# Display the updated dictionary
print("\nUpdated List:")
for name, fact in people.items():
    print(name + ":", fact)


PROJECT 2:-  (LIST)

# Store the scores in a list
scores = [2, 3, 6, 6, 5]

# Remove duplicate scores
scores = list(set(scores))

# Sort the list
scores.sort()

# Print the runner-up score
print("Runner-up score:", scores[-2])


PROJECT 3:- (DICTIONARY AND LIST)

# Dictionary containing student names and their marks
students = {
    "Krishna": [67, 68, 69],
    "Arjun": [70, 98, 63],
    "Malika": [52, 56, 60]
}

# Take student name as input
name = input("Enter a name: ")

# Calculate average marks
average = sum(students[name]) / len(students[name])

# Display average marks
print("Average percentage mark:", average)


PROJECT 4:- (STRING)

# Input the string
text = input("Enter a string: ")

# Count the occurrences of "Alex"
count = text.count("Alex")

# Display the result
print("Number of times Alex appears:", count)


MINI PROJECT FOR (FUNCTION/MODULES/PACKAGES)


